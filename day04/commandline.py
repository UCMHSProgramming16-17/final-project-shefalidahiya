# import sys so that I can use sys.argv
import sys

# sys. argv contains a list of every command line argument
# used when running the program
print(sys.argv)
print ("The first argument" addition.py[1])